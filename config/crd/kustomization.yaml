# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/app.kiegroup.org_kogitoinfras.yaml
- bases/app.kiegroup.org_kogitobuilds.yaml
- bases/app.kiegroup.org_kogitodataindices.yaml
- bases/app.kiegroup.org_kogitojobsservices.yaml
- bases/app.kiegroup.org_kogitomgmtconsoles.yaml
- bases/app.kiegroup.org_kogitoruntimes.yaml
- bases/app.kiegroup.org_kogitotrusties.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_kogitoinfras.yaml
#- patches/webhook_in_kogitobuilds.yaml
#- patches/webhook_in_kogitodataindices.yaml
#- patches/webhook_in_kogitojobsservices.yaml
#- patches/webhook_in_kogitomgmtconsoles.yaml
#- patches/webhook_in_kogitoruntimes.yaml
#- patches/webhook_in_kogitotrusties.yaml
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable webhook, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_kogitoinfras.yaml
#- patches/cainjection_in_kogitobuilds.yaml
#- patches/cainjection_in_kogitodataindices.yaml
#- patches/cainjection_in_kogitojobsservices.yaml
#- patches/cainjection_in_kogitomgmtconsoles.yaml
#- patches/cainjection_in_kogitoruntimes.yaml
#- patches/cainjection_in_kogitotrusties.yaml
# +kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
